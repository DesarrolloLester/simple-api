name: CI/CD Pipeline for Node.js API

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  # Job 1: Instalar dependencias y ejecutar pruebas automatizadas
  test:
    runs-on: ubuntu-latest
    steps:
      # Paso 1: Checkout del código fuente
      - name: Checkout code
        uses: actions/checkout@v2

      # Paso 2: Instalar Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # Versión de Node.js

      # Paso 3: Instalar dependencias
      - name: Install dependencies
        run: npm install

      # Paso 4: Ejecutar pruebas automatizadas
      - name: Run tests
        run: npm test  # Asegúrate de que tienes un script de prueba en tu package.json

  # Job 2: Escaneo de vulnerabilidades
  security_scan:
    runs-on: ubuntu-latest
    needs: test
    steps:
      # Paso 1: Checkout del código fuente
      - name: Checkout code
        uses: actions/checkout@v2

      # Paso 2: Instalar Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Paso 3: Instalar dependencias
      - name: Install dependencies
        run: npm install

      # Paso 4: Escanear vulnerabilidades con Snyk
      - name: Snyk vulnerability scan
        uses: snyk/actions/node@master
        with:
          args: test --severity-threshold=high
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}  # Agrega tu token Snyk en GitHub Secrets

  # Job 3: Construcción de la aplicación
  build:
    runs-on: ubuntu-latest
    needs: [test, security_scan]
    steps:
      # Paso 1: Checkout del código fuente
      - name: Checkout code
        uses: actions/checkout@v2

      # Paso 2: Instalar Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Paso 3: Instalar dependencias
      - name: Install dependencies
        run: npm install

      # Paso 4: Construir la aplicación
      - name: Build the application
        run: npm run build  # Asegúrate de tener un script de construcción en tu package.json


